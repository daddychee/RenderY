# memory_moi — memory file sinh ra TRÊN MÁY EDITOR

Claude Code máy editor: mỗi memory file mới/sửa → copy nguyên file `.md` vào đây (CLAUDE.md §7).
User máy gốc đọc kiểm → ưng thì copy vào memory máy gốc + thêm dòng `MEMORY.md`.
